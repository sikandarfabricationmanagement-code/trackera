{
  "entities": {
    "Restaurant": {
      "title": "Restaurant",
      "description": "Represents a single restaurant entity.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The unique name of the restaurant, used as the document ID."
        },
        "managerEmail": {
          "type": "string",
          "description": "The email address of the restaurant's manager.",
          "format": "email"
        },
        "branchesCount": {
          "type": "number",
          "description": "The total number of branches for this restaurant."
        }
      },
      "required": ["name", "managerEmail", "branchesCount"]
    },
    "Branch": {
      "title": "Branch",
      "description": "Represents a branch of a restaurant.",
      "type": "object",
      "properties": {
        "branchNumber": {
          "type": "number",
          "description": "The branch number, used as the document ID."
        },
        "staffCount": {
          "type": "number",
          "description": "The number of staff members in this branch."
        }
      },
      "required": ["branchNumber", "staffCount"]
    },
    "Staff": {
      "title": "Staff",
      "description": "Represents a staff member at a restaurant branch.",
      "type": "object",
      "properties": {
        "staffId": {
          "type": "string",
          "description": "The unique identifier for the staff member, used as the document ID."
        },
        "role": {
            "type": "string",
            "description": "Role of the staff member (e.g., Waiter, Cashier)"
        }
      },
      "required": ["staffId", "role"]
    }
  },
  "auth": {
    "providers": ["password", "anonymous"]
  },
  "firestore": {
    "/restaurants/{restaurantId}": {
      "schema": { "$ref": "#/entities/Restaurant" },
      "description": "Stores high-level information for each restaurant. The document ID is the restaurant's unique name."
    },
    "/restaurants/{restaurantId}/branches/{branchId}": {
      "schema": { "$ref": "#/entities/Branch" },
      "description": "Stores branch information for a specific restaurant. The document ID is the branch number."
    },
    "/restaurants/{restaurantId}/branches/{branchId}/staff/{staffId}": {
      "schema": { "$ref": "#/entities/Staff" },
      "description": "Stores staff member information for a specific branch. The document ID is the auto-generated staff ID."
    }
  }
}
